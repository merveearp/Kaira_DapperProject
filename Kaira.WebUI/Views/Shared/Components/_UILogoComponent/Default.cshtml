@model List<ResultLogoDto>
@*
   _UILogoComponent
*@
<section class="logo-bar py-5 my-5">
    <div class="container">
        <div class="logo-content">

            @foreach (var item in Model)
            {
                <div class="logo-item">
                    <img src="@item.ImageUrl"
                         alt="logo"
                         class="logo-image img-fluid">
                </div>
            }

        </div>
    </div>
</section>
